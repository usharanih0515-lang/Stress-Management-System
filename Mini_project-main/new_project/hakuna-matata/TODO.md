# TODO: Update MongoDB to MongoDB Atlas

- [x] Add mongoose dependency to backend/package.json
- [x] Update backend/server.js for MongoDB Atlas connection (if needed) - Already uses MONGO_URI
- [ ] Add MONGO_URI to backend/.env (user action: add your MongoDB Atlas connection string)
- [x] Install dependencies and test server startup (server will fail without MONGO_URI)
